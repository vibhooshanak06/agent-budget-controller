# Backend API base URL (must include /api/v1)
# In dev, the Vite proxy handles this — leave blank to use /api/v1 via proxy
VITE_API_URL=

# Backend Socket.IO server URL (WebSocket)
VITE_SOCKET_URL=http://localhost:4000
